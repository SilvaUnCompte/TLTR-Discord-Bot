# Discord Bot Configuration
# Copy this file to .env and fill in your actual values


# ========== DISCORD SETTINGS ==========

# Your bot's token from Discord Developer Portal
DISCORD_TOKEN=your_bot_token_here

# Your application's client ID from Discord Developer Portal (Application ID)
CLIENT_ID=your_client_id_here

# Optional: Guild ID for faster command deployment during development (Optional)
# Remove or leave empty to deploy commands globally
GUILD_ID=your_guild_id_here


# ========== AI SETTINGS ==========

# Groq API Key for AI features (get it from https://console.groq.com)
GROQ_API_KEY=gsk_tokenjusthere
GROQ_MODEL=meta-llama/llama-4-maverick-17b-128e-instruct

# Path to google-credentials.json for Google Cloud authentication
GOOGLE_APPLICATION_CREDENTIALS="google-credentials.json"


# ========== AUDIO SETTINGS ==========

# Threshold for minimum audio buffer size to consider (in bytes)
BUFFER_THRESHOLD=5000

# Minimum speech duration in milliseconds (ms) to avoid false triggers
MIN_SPEECH_DURATION=800

# Minimum volume threshold (RMS value) to detect actual speech vs noise
MIN_VOLUME_THRESHOLD=500

# Duration to wait for silence before stopping recording (in ms)
SILENCE_DURATION=1500


# ===== STT OPTIMIZATION SETTINGS =====

# Noise gate threshold for STT preprocessing (lower = more sensitive)
STT_NOISE_GATE_THRESHOLD=500